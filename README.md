# The Apocrypha Website🍷

This is where everything from Apocrypha gets showcased.

## Build Instructions

Obviously, you'll need [Node.js](https://nodejs.org) installed. Then follow the
build commands below:

```
npm i
npm run dev
```
